Ticket 2: Build the product lock loader.

Read the files in product_lock/.
Create validation for:
- exactly 12 variables
- exactly 24 assessment areas/drivers
- metric count totals 121
- variable weights total 100%
- every driver maps to one variable
- every metric maps to a driver and variable where data is available

Add tests. Run tests.
Do not build UI.
Do not generate reports yet.
