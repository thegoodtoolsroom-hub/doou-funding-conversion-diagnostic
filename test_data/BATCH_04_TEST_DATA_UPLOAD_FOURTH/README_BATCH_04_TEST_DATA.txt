BATCH 04 — TEST DATA — UPLOAD FOURTH

Purpose: These files prove whether the app really works.

Upload the batch CSV and XLSX into:
test_data/

The app must process the batch file and create 5 different result patterns.

Required proof:
- 5 organisations detected
- 5 report_data.json files created
- 5 different variable score patterns
- no all-50 default reports
- protected evidence flags appear where expected
- unsafe route/manual review flags appear where expected
- batch index created
- DOCX and PDF reports created or clear QA error shown

If all reports look the same, reject the build.
